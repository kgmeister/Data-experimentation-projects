# === Model / API ===
API_URL=https://api-inference.bitdeer.ai/v1/chat/completions
API_KEY=YOUR_API_KEY_HERE
MODEL=openai/gpt-oss-120b

# === Data directory (set this explicitly) ===
# Linux/macOS (including WSL):
# DATA_DIR=/mnt/data
# Windows alternative (uncomment this and comment the line above if on Windows):
DATA_DIR=C:\Team_Cashew_Synthetic_Data

FLASK_SECRET=change-me

# === App-specific ports (no conflicts) ===
NIBBLES_PORT=5001
CLARITY_PORT=5000

# (Optional) Global fallback port used if the app-specific port is not set:
# PORT=8000


# LLM (OpenAI-compatible)
LLM_API_URL=https://api-inference.bitdeer.ai/v1/chat/completions
LLM_API_KEY=YOUR_API_KEY_HERE
LLM_MODEL=openai/gpt-oss-120b